apiVersion: v1
kind: Service
metadata:
  name: todoapp
  labels:
    todoapp
spec:
  selector:
    app.kubernetes.io/name: todoapp
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http